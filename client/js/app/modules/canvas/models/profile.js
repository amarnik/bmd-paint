define( ['underscore', 'backbone' ], function( _, Backbone ) {
	var init = function(sandbox){
		// add all the views
		sandbox.models.Profile = Backbone.Model.extend({
			
		});
		
		
		// more view here ...
		
		
	};		
	return init;
});