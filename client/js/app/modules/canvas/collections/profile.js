define( ['underscore', 'backbone' ], function( _, Backbone ) {
	var init = function(sandbox){
		// add all the views
		sandbox.collections.Profiles = Backbone.Collection.extend({
			
		});
		
		
		// more view here ...
		
		
	};		
	return init;
});